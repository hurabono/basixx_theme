<div class="{{ class | default: '' }}" style="{{ style | default: '' }}">
  <div class="w-full md:px-6 lg:px-12 flex flex-col justify-center overflow-hidden" style="color:{{icon_color}}; height:16rem;">
    {% if quotation_show == true %}
    {% render 'icon-quotation-marks', class: 'w-5 h-auto mx-auto mb-8' %}
    {% endif %}
    
    <div class="text-center mb-5 text-sm" style="color: {{subtext_color}};">
      {{star_rating_box}}
    </div>

    <div class="text-center mb-6" style="color: {{text_color}};">
      {{ text }}
    </div>

    <div class="text-gray-600 text-sm text-center" style="color: {{subtext_color}};">
      — {{ author }}
    </div>
  </div>
</div>